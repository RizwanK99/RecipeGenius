package com.example.recipegenius.ui.ingredient;

import androidx.annotation.NonNull;

public enum MeasureUnit {
    GRAM, POUND, OUNCE, MILLILITER, FLUID_OUNCE, CUP;

    @NonNull
    @Override
    public String toString() {
        return super.toString();
    }
}
